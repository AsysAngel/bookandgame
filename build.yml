on: push
  jobs:
    build-node:
      runs-on: ubuntu-latest
      container: node:16
      steps:
        - run: node --version
        - run: npm --version
        - uses: actions/setup-node@v4
        - run: npm install
        - run: node server.js
